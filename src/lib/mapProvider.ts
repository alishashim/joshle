// Keep the hosted style choice here; the game and globe do not depend on its URL.
export const mapProvider = {
  name: 'OpenFreeMap',
  styleUrl: 'https://tiles.openfreemap.org/styles/positron',
} as const;
